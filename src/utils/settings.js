export const settingAgeSelect = {
  control: (base, state) => ({
    ...base,
    // match with the menu
    borderRadius: "8px",
  }),
};
export const settingGenderSelect = {
  control: (base, state) => ({
    ...base,
    // match with the menu
    borderRadius: "8px",
  }),
};
export const settingEmotionSelect = {
  control: (base, state) => ({
    ...base,
    // match with the menu
    borderRadius: "8px",
  }),
};
